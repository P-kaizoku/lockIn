{
  "name": "my-electron-app",
  "version": "1.0.0",
  "description": "",
  "main": "electron/main.js",
  "scripts": {
    "dev": "cross-env VITE_DEV_SERVER_URL=http://localhost:5173 concurrently \"npm run dev:vite\" \"npm run dev:electron\"",
    "dev:vite": "npm run dev --prefix frontend",
    "dev:electron": "electron .",
    "build": "npm run build:vite",
    "build:vite": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "cross-env": "^7.0.3",
    "electron": "^31.0.0"
  },
  "dependencies": {
    "better-sqlite3": "^12.2.0"
  }
}
